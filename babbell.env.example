# BabBell Environment Configuration
# Copy this file to babbell.env and fill in the values

# ============================================
# Option 1: Single Workspace (simpler)
# ============================================
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# ============================================
# Option 2: Multi Workspace (JSON array)
# ============================================
# Uncomment and configure if using multiple workspaces
# When WORKSPACES is set, SLACK_BOT_TOKEN and SLACK_APP_TOKEN are ignored
#
# WORKSPACES='[{"id": "team-a", "name": "Team A", "bot_token": "xoxb-...", "app_token": "xapp-..."}, {"id": "team-b", "name": "Team B", "bot_token": "xoxb-...", "app_token": "xapp-..."}]'

# Optional: Database path (default: ./babbell.db)
SQLITE_PATH=./babbell.db

# Optional: Cooldown between same button presses per user in seconds (default: 60)
COOLDOWN_SECONDS=60

# Optional: Include who clicked the button in broadcast message (default: false)
INCLUDE_ACTOR_IN_PUBLIC_MESSAGE=false

# Optional: Enable today's menu feature (default: false)
ENABLE_TODAYS_MENU=false

# Optional: Menu cache TTL in seconds (default: 600)
MENU_CACHE_TTL_SECONDS=600
