[project]
name = "babbell"
version = "1.0.0"
description = "Slack DM broadcast bot for meal notifications"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "slack-bolt>=1.18.0",
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
]
